#!/usr/bin/env ruby
# frozen_string_literal: true

# run - run the application CLI.

require "bundler/setup"

require_relative "../lib/hanami/cli/commands"

cli = cli = Dry::CLI.new(Hanami::CLI)
cli.()
